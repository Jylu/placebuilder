<div class="wire-top" data-published-at="{{publishedAtISO}}">
  <h3 class="{{wireCategory}}">{{wireCategoryName}}</h3>
  <p>{{publishedAt}}</p>
  {{#canEdit}} - <a href="#" class="editlink">EDIT</a>{{/canEdit}}
</div>
<div class="post-content">
  <img src="{{avatarUrl}}" />
  <div class="calendar avatar">
    <div class="month">{{short_month_name}}</div>
    <div class="day-of-month">{{day_of_month}}</div>
  </div>

  {{#time}}
  <div class="event-time">
    {{time}}
  </div>
  {{/time}}

  <h3>{{title}}</h3>
  <p class="author"><a {{#isFeed}} href="{{feedUrl}}" {{/isFeed}}>{{author}}</a></p>
  <p class="post-text">{{#markdown}}{{body}}{{/markdown}}</p>
  <table class="event-location">
    {{#venue}}<tr><th>Venue:</th><td>{{venue}}</td></tr>{{/venue}}
    {{#address}}<tr><th>Address:</th><td>{{address}}</td></tr>{{/address}}
  </table>

</div>
<div class="post-action clear">
  <ul class="actions">
    <li class="reply-link">Reply</li>
    <li class="message-link">Message {{first_name}}</li>
    <li class="share-link">Share</li>
    <li class="thank-link">Thank</li>
    <li class="flag-link">Flag</li>
   </ul>
  </ul>
</div>
<div class="post-interaction">
  <div class="interaction">
    {{#numThank}}
    <p class="thanked">
    <span class="blue">{{numThank}} {{peoplePerson}}</span> thanked this post: 
    {{#thanks}}
    <span class="blue">{{thanker}} </span>
    {{/thanks}}
    </p>
    {{/numThank}}
    {{#num_shares}}
    <!--
    <p class="publicized">
    <span class="blue">{{num_shares}} {{share_people}}</span> shared this post: 
    {{#shares}}
    <span class="blue">{{sharer}}</span>
    {{/shares}}
    </p>
    -->
    {{/num_shares}}
  </div>
</div>

<div class="replies-area"></div>

<div class="post-shadow">
  <span class="shadow_left"></span>
  <span class="shadow_right"></span>
</div>

<div class="clear">
</div>
