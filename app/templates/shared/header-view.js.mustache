<div id="nav_wrapper">
  
  <div class="community-name">
    <a href="{{root_url}}">
      {{#isAuth}}
        <h1>{{#hasCommunity}}
          {{community_name}}
        {{/hasCommunity}}</h1>
      {{/isAuth}}
      {{#notAuth}}
        <h1>OurCommonPlace</h1>
        {{#hasCommunity}}
          <span class="community">
            {{community_name}}
          </span>
        {{/hasCommunity}}
      {{/notAuth}}
    </a>
  </div>
  <div class="nav"></div>

</div>
