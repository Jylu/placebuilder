<div id="todo-viewer">
  <h3>Task List</h3>


  <select id="tag-list">
  <option>Tag List</option>
  {{#tags}}
  <option class="tag-adding">{{.}}</option>
  {{/tags}}
  </select>
  <button id="add-tag">Add tag</button>
  {{#todos}}
    <h4>{{.}}</h4>
    <ul class="todo-specific" value="{{.}}">
    {{#profiles}}
      <input type="checkbox" name="{{.}}">{{.}}<br>
    {{/profiles}}
    </ul>
  {{/todos}}
</div>
